name: CI/CD Pipeline to EC2

on:
  push:
    branches: [main] # Запускать при пуше в ветку main

jobs:
  deploy:
    runs-on: ubuntu-latest


    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        fetch-depth: 0 # Полная история для Git Submodules, если используются

    - name: Set up SSH
      uses: webfactory/ssh-agent@v0.5.3 # Более удобный способ настройки SSH
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

    - name: Deploy to Server via SSH
      env:
        SSH_HOST: ${{ secrets.SSH_HOST }}
        SSH_USERNAME: ${{ secrets.SSH_USERNAME }}
        REPO_REMOTE_URL: "https://github.com/zhenyapetko/portfolio-site.git" # Замените на публичный URL вашего репозитория

      run: |
        set -e # Выйти немедленно, если любая команда завершится неудачей

        echo "Connecting to ${{ env.SSH_USERNAME }}@${{ env.SSH_HOST }}"

        # Определение всех команд, которые нужно выполнить на удаленном сервере,
        # в виде одной большой строки.
        # Здесь мы используем символы новой строки (\n) для разделения команд
        # и экранируем кавычки, если они нужны внутри команд.
        SSH_SCRIPT="
          REPO_PATH=\"/opt/portfolio-site\" # Путь, где Docker Compose проект на сервере
          
          # --- Подготовка репозитория ---
          if [ ! -d \"\$REPO_PATH\" ]; then
            echo \"Cloning repository to \$REPO_PATH...\"
            git clone \"\$REPO_REMOTE_URL\" \"\$REPO_PATH\" || { echo \"Failed to clone repository\"; exit 1; }
          fi

          echo "Cleaning up old Docker resources..."
        
          docker system prune -f  # Удаляет неиспользуемые (dangling) образы, контейнеры, сети и build cache
          docker volume prune -f  # Удаляет неиспользуемые тома
          docker builder prune -f
          
          echo \"Entering project directory: \$REPO_PATH\"
          cd \"\$REPO_PATH\" || { echo \"Failed to change directory to \$REPO_PATH\"; exit 1; }

          echo \"Pulling latest changes from Git...\"
          git pull origin main || { echo \"Failed to pull latest changes\"; exit 1; }
          
          # --- Docker Compose действия ---
          echo \"Stopping existing services...\"
          docker-compose down || echo \"No running services to stop or failed gracefully.\"
          
          echo \"Building services...\"
          docker-compose build --no-cache || { echo \"Failed to build Docker images\"; exit 1; }
          
          echo \"Starting services...\"
          docker-compose up -d --force-recreate || { echo \"Failed to start Docker Compose services\"; exit 1; }
          
          echo \"Deployment successful.\"
        "
        # Передача скрипта одной строкой в SSH, выполняя обрезку пробелов
        # и выполнение как Bash-скрипта на удаленном сервере.
        # Telegram сообщения здесь не должны быть!
        ssh -o StrictHostKeyChecking=no ${{ env.SSH_USERNAME }}@${{ env.SSH_HOST }} "bash -c '$(echo "$SSH_SCRIPT" | sed -e 's/^[[:space:]]*//')'"

    # --- Telegram-уведомления теперь как отдельные шаги, как и должны быть ---
    - name: Send Telegram Notification (Success)
      if: success()
      uses: appleboy/telegram-action@master
      with:
        to: ${{ secrets.TG_CHAT_ID }}
        token: ${{ secrets.TG_BOT_TOKEN }}
        format: markdown # Указываем формат Markdown
        message: |
          ✨ *Deployment Successful!* ✨
          
          ---
          
          🚀 *Проект:* `{{ github.repository }}`
          🌿 *Ветка:* `{{ github.ref_name }}`
          📦 *Коммит:* `{{ github.sha }}`
          
          ---
          
          🌐 *Ваш сайт доступен:*
          [Перейти на сайт](http://${{ secrets.SSH_HOST }})
          
          📊 *Мониторинг Grafana:*
          [Grafana Dashboard](http://${{ secrets.SSH_HOST }}:3000)
          
          📈 *Мониторинг Prometheus:*
          [Prometheus Status](http://${{ secrets.SSH_HOST }}:9090)
          
          ---
          
          ✅ *Все сервисы запущены и работают!*
    
    - name: Send Telegram Notification (Failure)
      if: failure()
      uses: appleboy/telegram-action@master
      with:
        to: ${{ secrets.TG_CHAT_ID }}
        token: ${{ secrets.TG_BOT_TOKEN }}
        format: markdown # Также указываем формат Markdown для ошибок
        message: |
          🚨 *Deployment Failed!* 🚨
          
          ---
          
          ❌ *Проект:* `{{ github.repository }}`
          🌿 *Ветка:* `{{ github.ref_name }}`
          📦 *Коммит:* `{{ github.sha }}`
          
          ---
          
          🧐 *Пожалуйста, проверьте логи:*
          [GitHub Actions Logs](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
          
          ---
          
          ⚠️ *Возникла ошибка во время деплоя. Срочно примите меры!*